---
title: 'Integration with Claude'
description: 'How to connect VeyraX to Claude via MCP'
---

Claude is a powerful AI assistant by Anthropic that can be enhanced with VeyraX integration to access your tools directly.

## Automatic Installation

### Run Installation Command

Open your terminal and run the following command:

```bash
npx -y @smithery/cli@latest install @VeyraX/veyrax-mcp --client claude
```

### Add API Key

When prompted, provide the API key that you created in the [VeyraX Platform](https://veyrax.com/agent/new).

## Manual Installation

### Access Claude Settings

Open Claude and navigate to settings.

![Screen 1](/images/claude/claude_1.png)

### Open Developer Settings
Then open developer settings, which will open a folder on your computer.

![Screen 2](/images/claude/claude_2.png)

### Locate Configuration File

Find and open the file called `claude_desktop_config.json`.

![Screen 3](/images/claude/claude_3.png)

### Add MCP Configuration

Add the following configuration to your MCP config file. You'll need to paste the command with your VeyraX API key that you can copy from the [VeyraX Platform](https://www.veyrax.com/agent/new).

The configuration will look like this:

```json
{
  "mcpServers": {
    "@VeyraX-veyrax-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "@smithery/cli@latest",
        "run",
        "@VeyraX/veyrax-mcp",
        "--config",
        "\"{\\\"VEYRAX_API_KEY\\\":\\\"YOUR_VEYRAX_API_KEY\\\"}\""
      ]
    }
  }
}
```

## Restart Claude Application

After you add new tools, you need to restart the app to access them. You'll see the available tools in the next launch.

![Using VeyraX in Claude](/images/claude/claude_4.png)


## Start Using VeyraX in Claude

Now you can ask questions about any tools you've connected via VeyraX directly in Claude.

![Using VeyraX in Claude](/images/claude/claude_5.png)